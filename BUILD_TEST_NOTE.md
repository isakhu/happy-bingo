# Windows test build

Use branch `fix/systematic-audit-20260820` for the current audited test build.

The code changes in this branch are intended for the authentication/startup and offline reliability test. Do not treat `main` as the tested result until the Windows installer has passed the checklist in `AUDIT_TEST_CHECKLIST.md`.
