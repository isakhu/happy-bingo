// Static presentation-only patch for the Cartella checking/winner UI.
// The current Cartella CHECK presentation is owned by check-overrides.css.
// Keep this file intentionally free of competing CardInspector styles so the
// current light/gold/green layout is the only active presentation layer.
export {}
